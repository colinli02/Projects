# mkdocs project page

Built using ```pip install mkdocs-material```

## To view locally

1. cd to current directory
2. .\serve.bat
3. View on browser on hosted port url

## To Update

```cmd
mkdocs build
```

- CI has auto deploy using github actions, builds on push.
